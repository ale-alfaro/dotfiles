
[settings]
experimental = true
python.uv_venv_auto = "source"
# python.uv_venv_create_args = ['--clear']
# configure how frequently (in minutes) to fetch updated plugin repository changes
# this is updated whenever a new runtime is installed
# (note: this isn't currently implemented but there are plans to add it: https://github.com/jdx/mise/discussions/6735)
plugin_autoupdate_last_check_duration = '1 week' # set to 0 to disable updates
unix_default_file_shell_args = 'zsh -uc'
unix_default_inline_shell_args = 'zsh -uc'
# config files with these prefixes will be trusted by default
trusted_config_paths = ['~/sibel/eng/fw', '~/sibel/eng/tools']

verbose = false      # set to true to see full installation output, see `MISE_VERBOSE`
http_timeout = "30s" # set the timeout for http requests as duration string, see `MISE_HTTP_TIMEOUT`
jobs = 4             # number of plugins or runtimes to install in parallel. The default is `4`.
raw = true           # set to true to directly pipe plugins to stdin/stdout/stderr
yes = false          # set to true to automatically answer yes to all prompts

not_found_auto_install = true # see MISE_NOT_FOUND_AUTO_INSTALL
task_output = "prefix"        # see Tasks Runner for more information


[tools]
claude-code = "latest"
codex = "latest"
node = "latest"
"pipx:cmakelang" = { version = "latest", extras = "yaml,html-gen" }
"pipx:ptpython" = "latest"
"pipx:pudb" = "latest"
"pipx:pyrefly" = "latest"
"pipx:ruff" = "latest"
"pipx:ty" = "latest"
python = ["latest", "3.13"]
tree-sitter = "latest"
uv = "latest"
# [env]
# MISE_EXPERIMENTAL=1
